# Components

Shared UI components live here.

Rules:
- Keep only cross-client reusable components in this directory.
- Client-specific UI should stay inside `client/<feature>/ui`.
- Do not place server-only or data-loading logic here.
